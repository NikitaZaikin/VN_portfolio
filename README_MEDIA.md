# 🎬 Система управления медиа-файлами

## Быстрый старт

### 1. Создайте структуру папок

Для каждого проекта создайте папки в `public/`:

```
public/
  projects/
    1/
      photos/
        (ваши фото здесь)
      videos/
        (ваши видео здесь)
    2/
      photos/
      videos/
```

### 2. Используйте в проекте

**Вариант А: Автоматическое сканирование (проще всего)**

```typescript
// lib/data.ts
{
  id: "1",
  title: "Car Photoshoot",
  photosFolder: "/projects/1/photos",  // ✅ Все фото автоматически
  videosFolder: "/projects/1/videos",  // ✅ Все видео автоматически
  // Остальные поля...
}
```

**Вариант Б: Ручной контроль порядка**

```typescript
{
  id: "2",
  title: "Fashion Session",
  manualPhotos: [
    { path: "/projects/2/photos/main.jpg", order: 1, isMain: true },
    { path: "/projects/2/photos/photo2.jpg", order: 2 },
  ],
  // Остальные поля...
}
```

## Как это работает

1. **Автоматическое сканирование**: Просто укажите `photosFolder` или `videosFolder` - система сама найдет все файлы
2. **Ручное управление**: Используйте `manualPhotos`/`manualVideos` для контроля порядка
3. **Комбинирование**: Можно указать несколько файлов вручную, остальные подтянутся автоматически

## Поддерживаемые форматы

- **Фото**: `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`, `.svg`
- **Видео**: `.mp4`, `.webm`, `.mov`, `.avi`, `.mkv`

## Примеры

См. подробную документацию в `docs/MEDIA_MANAGEMENT.md`

